import Chai from "./Component";

function App() {
  return (
    <>
      <Chai/>
      <h1>This is react project using vite | Omkar</h1>
    </>//This is called a fragment. As React allows us to send only one element, we could have sent it with the div tag but it was later changed to a fragment.
  );
}

export default App
